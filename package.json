{
  "name": "unit-tests",
  "version": "0.1.0",
  "private": true,
  "description": "A testbed for unit testing Node",
  "scripts": {
    "test": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^26.4.2"
  }
}
